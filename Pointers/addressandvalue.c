#include <stdio.h>
#include <conio.h>
void main()
{
    int a, b;
    a = 5;
    b = 10;
    printf("\n The value of A is %d", a);
    printf("\n The address of A is %u", &a);
    printf("\n The value of B is %d", b);
    printf("\n The address of B is %u", &b);
    getch();
}